%h1 My Bids
%h3 Current Silent Auctions
- if @running_bids.empty?
  .empty_collection
    %p.emptyMsg You currently have not participated in any running auctions.
-else
  %table{:class => 'table table-simpleBordered', :id => 'currentBids'}
    %tbody
      - @running_bids.each do |bid|
        - auction = bid.silent_auction
        = render :partial => "silent_auctions/running_auction", :locals => {:auction => auction }

%h3 Current Normal Auctions
- if @running_bids.empty?
  .empty_collection
    %p.emptyMsg You currently have not participated in any running auctions.
-else
  %table{:class => 'table table-simpleBordered', :id => 'currentBids'}
    %tbody
      - @running_normal_bids.each do |bid|
        - auction = bid.silent_auction
        = render :partial => "silent_auctions/running_normal_auction", :locals => {:auction => auction }

%h3 Closed Auctions
- if @closed_bids.empty?
  .empty_collection
    %p.emptyMsg You don't have any bids in the closed auctions.
    -#%p.emptyMsg You don't have any winning bids in the closed auctions.
-else
  %table{:class => 'table table-simpleBordered', :id => 'closedBids'}
    %tbody
      - @closed_bids.each do |bid|
        - auction = bid.silent_auction
        = render :partial => "silent_auctions/close_auction", :locals => {:auction => auction }