%h1 Listing assets

%table
  %tr
    %th Data file name
    %th Data content type
    %th Data file size
    %th Attachable
    %th
    %th
    %th

  - @assets.each do |asset|
    %tr
      %td= asset.data_file_name
      %td= asset.data_content_type
      %td= asset.data_file_size
      %td= asset.attachable
      %td= link_to 'Show', asset
      %td= link_to 'Edit', edit_asset_path(asset)
      %td= link_to 'Destroy', asset, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Asset', new_asset_path
